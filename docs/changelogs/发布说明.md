# Word文档格式修复工具 - 更新日志

## v1.4.1 (2026-01-25)

### 主要更新

1. **可视化对比（Visual Diff）**
   - 支持 Docx 转 HTML 预览
   - 支持对比修改前/修改后内容，并生成差异视图（分栏/统一视图）
   - 新增差异统计（新增/删除）展示

2. **UI 与交互修复**
   - 统一“清除”按钮风格（新增 ghost 风格按钮与更明确的图标）
   - 补齐多处缺失样式（进度条、开发者日志、预设编辑器规则选择区等），提升整体一致性
   - 修复预设点击绑定异常（确保点击左侧预设可正确切换并应用）

### 测试结果

- 单元测试：通过

## v1.3.1 (2026-01-24)

### 主要更新

1. **架构升级**
   - 完全重构了项目架构，采用规则引擎模式
   - 分离了Python后端和Electron前端
   - 实现了前后端通过IPC通信
   - 优化了模块依赖关系，提高了代码可维护性

2. **功能增强**
   - 支持更多文档格式修复规则
   - 增加了多种预设配置
   - 实现了自定义预设功能
   - 支持批量处理文件
   - 生成详细的执行报告

3. **UI改进**
   - 升级到Electron GUI界面
   - 实现了响应式设计，支持窗口大小调整
   - 优化了用户交互体验
   - 提供了直观的规则配置界面

4. **打包与部署**
   - 实现了独立EXE安装包打包方案
   - 支持Windows平台构建
   - 优化了构建流程

### 技术变更

- 采用分层架构设计
- 实现了规则引擎模式
- 引入了Electron前端框架
- 优化了Python后端代码结构
- 实现了前后端分离的通信机制

### 测试结果

- 单元测试：通过
- 集成测试：通过
- UI界面测试：通过
- 功能测试：通过

## v1.1.2 (2026-01-13)

### 修复内容

1. **UI边框问题修复**
   - 消除了鼠标聚焦到窗口内部模块时显示的黑色线条
   - 移除了Canvas、Listbox、Text等控件的焦点边框
   - 优化了输入控件的边框样式，确保视觉一致性

2. **界面美化**
   - 为所有Frame组件添加了统一的样式配置
   - 调整了控件的边框宽度和样式，提升整体美观度
   - 确保控件在聚焦时不会显示突兀的黑色边框

### 技术变更

- 为TFrame添加了样式配置，设置borderwidth=0和relief='flat'
- 为Canvas控件添加了highlightthickness=0参数
- 优化了TEntry和TCombobox的样式设置
- 为Listbox和Text控件添加了highlightthickness=0参数
- 为所有相关控件设置了适当的边框宽度和样式

### 测试结果

- UI界面测试：通过
- 边框显示测试：通过
- 控件聚焦测试：通过
- 视觉一致性测试：通过

## v1.1.1 (2026-01-12)

### 修复内容

1. **布局优化**
   - 移除了固定宽度限制，内容现在可以充满整个窗口
   - 消除了两侧固定边距，彻底解决留白问题
   - 保持了内容的响应式调整能力

2. **视觉一致性**
   - 为画布设置了与内部框架一致的背景颜色
   - 解决了底部背景与内容区域颜色不一致的问题

### 技术变更

- 修改了 `_on_canvas_configure` 函数，移除了宽度限制和边距
- 将 `max_content_width` 设置为无限大，实现完全响应式布局
- 将 `padding` 设置为 0，消除所有固定边距
- 为 Canvas 组件添加了背景颜色设置

### 测试结果

- 性能测试：通过
- 配置默认值测试：通过
- 工具函数测试：全部通过
- UI初始化测试：通过
- 预设配置测试：因环境问题未执行（与代码修改无关）

### 使用建议

- 窗口现在可以充分利用整个屏幕空间
- 内容会自动适应不同大小的窗口
- 界面更加整洁，没有不必要的留白

## 如何升级

1. 从 GitHub 拉取最新代码
2. 重新安装依赖（如果需要）
3. 运行应用程序

---

## v1.1.0

### 初始版本
- 基础文档修复功能
- 多种预设配置
- 批量处理支持
- 日志记录功能
